import Tooltip from 'react-bootstrap/Tooltip';

// Add props and types, interface if necessary
function ProfileTooltip() {
    
    return <>
            <Tooltip id="button-tooltip-2">This message will depend on props passed down from the profile{}</Tooltip>
        </>

}

export default ProfileTooltip